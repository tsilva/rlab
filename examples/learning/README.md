# Learning Examples

This folder contains standalone scripts for studying reinforcement-learning mechanics.

Use these scripts as readable experiments, not as the production training path. Production runs should use checked-in goal recipes through `rlab train`.
