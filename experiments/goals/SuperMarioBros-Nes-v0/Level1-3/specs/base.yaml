defaults:
- ../../Level1-1/specs/base@_global_
- ../_goal@goal
- _self_
spec_id: base
template_vars:
  batch_id: b273
  recipe: b55
  recipe_label: B55 low-KL late-decay
  phase: transfer
description: >-
  Seed {seed} runs the {goal_id} transfer of the successful Level1-1 {recipe_label} recipe,
  created to test whether that policy recipe transfers without changing PPO or reward
  hyperparameters while overriding only {goal_id} identity and reporting metadata.
wandb_tags:
- "goal_id:{goal_id}"
- "spec_id:{spec_id}"
- "env_id:{env_id}"
- "recipe:{recipe}"
- "phase:{phase}"
