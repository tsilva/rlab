defaults:
- ../../Level1-1/specs/base@_global_
- ../_goal@goal
- _self_
slug: base
parent_spec_slug: base
template_vars:
  batch_id: b273
  recipe: b55
  recipe_label: B55 low-KL late-decay
hypothesis: >-
  The successful Level1-1 low-KL late-decay recipe should transfer to {goal_id} without
  changing PPO or reward hyperparameters. This spec inherits the Level1-1 policy recipe and
  overrides only {goal_id} identity, grouping, and reporting metadata.
wandb_group: "{batch_id}-{level_short}-{recipe}-transfer"
run_name_label: "{level_short}-{recipe}-transfer"
wandb_tags:
- "{level_tag}"
- "{recipe}"
- lowkl
- lrdecay
- transfer
run_description_template: "{goal_id} transfer of the successful Level1-1 {recipe_label} recipe, seed {seed}."
