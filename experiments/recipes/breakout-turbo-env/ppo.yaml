defaults:
- ../_presets/atari-ppo@_global_
- _self_
recipe_id: ppo
description: >-
  Seed {seed} trains a native-vector Breakout Turbo PPO policy for {goal_id} under the
  goal-owned observation, action, reward, and termination contract until the fixed timestep cap;
  checkpoint and final evaluation are disabled.
