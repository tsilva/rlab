interface:
  display_name: "Launch Experiment"
  short_description: "Launch and follow rlab runs through completion"
  default_prompt: "Use $launch-experiment to launch this training recipe on beast-3 and keep monitoring every run until its terminal event."
